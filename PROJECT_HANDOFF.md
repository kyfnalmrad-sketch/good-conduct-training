# تسليم ومتابعة مشروع نظام إصدار حسن السيرة والسلوك

## حالة المشروع

هذا المشروع نموذج تدريبي ببيانات وهمية فقط. يحتوي على واجهة إدخال للبيانات، معاينة ثنائية اللغة، رفع صورة تجريبية، توليد QR، وتحديث الرموز عند تغيير الرقم المرجعي والرقم الداخلي.

القالب البصري الأساسي مأخوذ من ملف Word المقدم من فريق المشروع. يجب التعامل مع الترويسة والشعار والبسملة والعلامة المائية كعناصر ثابتة، بينما يمكن تعديل منطقة البيانات والجدول ومواقع المتغيرات بعد موافقة الفريق.

## قابلية التعديل

المشروع قابل للتعديل من فريق التطوير أو أي مساعد برمجي آخر لديه وصول فعلي إلى مستودع GitHub وبيئة المشروع. يمكن تعديل ملفات الواجهة والمكونات والتنسيقات، ثم تشغيل الفحوصات قبل حفظ أي نسخة جديدة.

أي مساعد آخر يتابع المشروع يجب أن يقرأ هذا الملف وملف `README.md`، ثم يراجع صورة المرجع وقالب Word قبل تعديل التصميم. لا يُفترض أن يعيد تصميم الوثيقة من الصفر؛ يجب الحفاظ على القالب المعتمد وعدم تغيير الترويسة أو الشعار أو البسملة أو العلامة المائية دون موافقة الفريق.

## رمز التفويض التدريبي

لأغراض العرض المحلي فقط، يمكن استخدام رمز التفويض التدريبي التالي داخل بيئة الاختبار:

> `7777`

هذا الرمز **ليس كلمة مرور ولا وسيلة أمنية**، ولا يمنح صلاحية إلى GitHub أو الخادم أو ملفات المشروع، ولا ينبغي استخدامه للتحكم في الوصول الحقيقي. الصلاحيات الفعلية تُدار من خلال حسابات GitHub، ودعوات المتعاونين، وبيانات اعتماد منفصلة محفوظة خارج المستودع.

إذا استُخدم الرمز في واجهة تدريبية، فيجب أن تكون الوظيفة واضحة بأنها محاكاة محلية، وألا تُربط بأي عملية نشر أو وصول إلى بيانات حقيقية.

## قواعد البيانات والخصوصية

يجب استخدام بيانات وهمية في التطوير والاختبار. يمنع إدخال أو رفع بيانات مواطنين حقيقية أو صور شخصية حقيقية إلى هذا المستودع. كما يمنع تخزين مفاتيح API أو كلمات المرور أو رموز الوصول داخل ملفات المشروع أو ملفات Markdown.

## اختبار التعديلات

بعد أي تعديل، شغّل `pnpm check` ثم `pnpm build`. راجع المعاينة بصريًا، وتحقق من أن تغيير الرقم المرجعي أو الرقم الداخلي يحدث QR والباركود تلقائيًا، وأن الصورة والجدول والترويسة لا تتداخل. لا تُحفظ نسخة جديدة قبل نجاح الفحص والمراجعة.

## مسار التطوير المقترح

الخطوة التالية هي مراجعة مواضع الترويسة والبيانات والرموز مع الفريق، ثم تثبيت إخراج PDF. بعد اعتماد PDF يمكن إضافة إخراج Word قابل للتعبئة، مع إبقاء القالب الأصلي مصدر التصميم.

## ملاحظة للصلاحيات

وجود هذا الملف لا يمنح أي مساعد أو مستخدم صلاحية فعلية. متابعة العمل تتطلب وصولًا حقيقيًا إلى مستودع GitHub أو إلى بيئة المشروع، وفق الصلاحيات التي يمنحها مالك المشروع.
