# تعليمات نظام إصدار حسن السيرة والسلوك

## الاستخدام العام

النظام مخصص لإدخال بيانات وثيقة حسن السيرة والسلوك، ثم معاينتها وإخراجها بصيغة PDF على صفحة A4. استخدم بيانات اختبارية أثناء التطوير، ولا ترفع بيانات مواطنين حقيقية أو صورًا شخصية حقيقية إلى المستودع.

## قالب Excel

اضغط **تحميل قالب Excel** من صفحة المحرر. يضم الملف ورقتين:

| الورقة | الاستخدام |
|---|---|
| `Good Conduct` | إدخال بيانات شخص واحد في الصف الثاني فقط. يجب إبقاء أسماء الأعمدة كما هي. |
| `Instructions` | شرح الحقول والتواريخ والصور وما ينشئه النظام تلقائيًا. |

استخدم صيغة `DD/MM/YYYY` أو `YYYY-MM-DD` للتواريخ. يمكن استخدام العربية أو الإنجليزية في الحقول الثنائية. عند الاستيراد يقرأ النظام الصف الأول من البيانات فقط.

لا تُدخل أرقام القيد أو الرقم المرجعي أو الرقم الداخلي أو رقم الإصدار في Excel؛ هذه الأرقام ينشئها النظام تلقائيًا بشكل غير مكرر، ويستخدمها في QR والباركود.

## مواصفات الصورة

- **الصورة الموصى بها:** 400 × 600 بكسل بنسبة 2:3.
- **الصيغ المقبولة:** JPG أو PNG وأي صيغة صورة يدعمها المتصفح.
- **الحجم الأقصى:** 5MB.
- **المقاس داخل الوثيقة:** 40 × 60mm.
- يجب أن تكون الصورة واضحة ومواجهة للأمام وبدون قص زائد حول الشخص.

## العلامة المائية

عند تفعيل خيار **إضافة العلامة المائية** يستخدم النظام نسخة مستقلة من الصورة، ويزيل الخلفية البيضاء المتصلة منها تلقائيًا، ثم يعرضها بشفافية فوق القالب. لا يتم تعديل الصورة الشخصية الأصلية بسبب معالجة العلامة المائية.

مقاس العلامة المائية التقريبي داخل الوثيقة هو 24 × 36mm. للحصول على أفضل نتيجة، استخدم صورة بخلفية بيضاء متجانسة أو شفافة أصلًا.

## PDF والطباعة

من صفحة المعاينة اضغط **تصدير PDF** أو استخدم خيار الطباعة في المتصفح، ثم اختر:

- حجم الورق: `A4`.
- الاتجاه: `Portrait`.
- الهوامش: `None` أو `0` إن كان الخيار متاحًا.
- مقياس الطباعة: `100%`.
- فعّل طباعة الخلفيات إذا ظهر هذا الخيار.

يجب أن تكون النتيجة صفحة واحدة A4. الصورة والباركود موضوعان أسفل الخط العلوي بمقاس ثابت.

## الباركود وQR

الباركود الشريطي المستخدم هو **Code128** وليس EAN-14، لأنه يستطيع حمل الأرقام المرجعية الحالية التي قد تحتوي على حروف، مثل `REF-...` و`INT-...`. يحتوي الباركود على رقم القيد والرقم المرجعي والرقم الداخلي ورقم الإصدار، ولونه ذهبي مطابق للون الشعار.

رمز QR يحتوي بيانات التعريف الأساسية باللغة الإنجليزية. عند تغيير بيانات الشخص أو الأرقام النظامية يعاد توليد QR والباركود تلقائيًا.

## التحقق قبل الرفع

بعد أي تعديل شغّل:

```bash
pnpm check
pnpm build
```

ثم راجع المعاينة وملف PDF الفعلي، وتأكد من عدم تداخل الصورة أو الباركود أو العلامة المائية مع الخط العلوي أو الجدول.

## ملاحظة GitHub

لا يتم رفع أي تعديل إلى GitHub إلا بعد مراجعة المعاينة ونجاح الفحص والبناء واعتماد النسخة النهائية.
